<?xml version="1.0" encoding="utf-8"?>
<!-- Modify this file to customize your launch splash screen -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />

    <!-- App icon shown on the native pre-Flutter launch screen so the app
         doesn't look like a blank white screen during startup. Uses the
         flat foreground PNG rather than @mipmap/ic_launcher — that mipmap
         resolves to an <adaptive-icon> XML on API 26+, which a plain
         <bitmap> here can't center correctly (it renders pinned to the
         top). -->
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/ic_launcher_foreground" />
    </item>
</layer-list>
